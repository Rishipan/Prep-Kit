#include<iostream>
using namespace std;

void addTwoBits(string s1, string s2){
    bool carry = false;
    int i = s1.length(), j = s2.length();
    string res = "";
    
    while(i and j){
        if(carry){
            if(s1[i-1] == '1')
        }
    }
}

int main(){
    stirng s1, s2;
    cin >> s1 >> s2;

    cout << addTwoBits(s1, s2);

    return 0;
}